import { ToSiteTimePipe } from './to-site-time.pipe';

describe('ToSiteTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ToSiteTimePipe();
    expect(pipe).toBeTruthy();
  });
});
