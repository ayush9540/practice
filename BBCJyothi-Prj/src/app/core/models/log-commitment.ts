export interface LogCommitment {
    title: string,
    warningDate: string,
    commitDate: string,
    condition: string,
    originalDate: string,
    completeDate: string,
    of: string
}
