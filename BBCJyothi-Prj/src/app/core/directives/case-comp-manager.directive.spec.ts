import { CaseCompManagerDirective } from './case-comp-manager.directive';

describe('CaseCompManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseCompManagerDirective();
    expect(directive).toBeTruthy();
  });
});
